# HaiLanGo 引き継ぎドキュメント

**作成日**: 2025-12-05
**目的**: ローカル環境での GCP インフラ設定の継続

---

## 1. 現在の状況

### 完了済み
- [x] Terraform 設定ファイル一式の作成
- [x] GCP 管理用シェルスクリプトの作成
- [x] `.env.example` の GCP 設定追加
- [x] GitHub へのコミット・プッシュ完了

### 未完了（ローカルで実行が必要）
- [ ] `gcloud auth login` による認証
- [ ] `gcloud auth application-default login` による認証
- [ ] `terraform init` の実行
- [ ] `terraform apply` の実行

---

## 2. GCP プロジェクト情報

```
プロジェクト ID: hailango
プロジェクト番号: 1077128828066
リージョン: asia-northeast1 (東京)
月額予算: $50 USD
```

---

## 3. 次のステップ（ローカル環境で実行）

### 3.1 リポジトリの取得

```bash
git pull origin main
# または
git clone https://github.com/clearclown/HaiLanGo.git
cd HaiLanGo
```

### 3.2 Google Cloud 認証

```bash
# gcloud CLI がインストールされていない場合
# https://cloud.google.com/sdk/install からインストール

# 認証（ブラウザが開きます）
gcloud auth login

# アプリケーションデフォルト認証（Terraform用）
gcloud auth application-default login

# プロジェクト設定
gcloud config set project hailango
```

### 3.3 Terraform の実行

```bash
cd infrastructure

# 方法1: Makefile を使用（推奨）
make setup-gcloud      # GCP初期セットアップ（API有効化など）
make terraform-init    # Terraform初期化
make terraform-plan    # 変更内容の確認
make terraform-apply   # 変更の適用

# 方法2: 手動実行
cd terraform
terraform init
terraform plan
terraform apply
```

### 3.4 認証情報の取得

```bash
cd infrastructure

# サービスアカウントキーの取得
make get-credentials
# → infrastructure/credentials.json に保存される

# .env ファイルの生成
make env-file
# → backend/.env.gcloud に GCP 設定が出力される
```

---

## 4. 作成済みファイル一覧

### Terraform 設定 (`infrastructure/terraform/`)

| ファイル | 説明 |
|---------|------|
| `main.tf` | GCP API の有効化（Vision, TTS, STT, Translation 等） |
| `variables.tf` | 変数定義（プロジェクトID、リージョン、予算等） |
| `budget.tf` | 予算アラート（50%, 80%, 90%, 100% 閾値） |
| `api_keys.tf` | サービスアカウント、APIキー、IAMロール |
| `storage.tf` | Cloud Storage バケット（ライフサイクルルール付き） |
| `quotas.tf` | API 使用量モニタリング、アラート |
| `outputs.tf` | 出力定義（.env 生成用） |
| `terraform.tfvars.example` | 変数設定例 |

### シェルスクリプト (`infrastructure/scripts/`)

| ファイル | 説明 |
|---------|------|
| `setup-gcloud.sh` | GCP 初期セットアップ（API有効化、SA作成） |
| `create-budget.sh` | 月額予算の作成 |
| `check-costs.sh` | 現在のコスト確認 |

### Makefile コマンド

```bash
make help              # ヘルプ表示
make setup-gcloud      # GCP初期セットアップ
make create-budget     # 予算作成
make check-costs       # コスト確認
make terraform-init    # Terraform初期化
make terraform-plan    # 変更内容確認
make terraform-apply   # 変更適用
make terraform-destroy # リソース削除（注意！）
make env-file          # .env ファイル生成
make get-credentials   # サービスアカウントキー取得
```

---

## 5. 設定される GCP リソース

### 有効化される API
- Cloud Vision API（OCR）
- Cloud Text-to-Speech API
- Cloud Speech-to-Text API
- Cloud Translation API
- Cloud Storage
- Cloud Billing API
- Cloud Monitoring API
- Secret Manager API
- IAM API

### Cloud Storage バケット
| バケット名 | 用途 | ライフサイクル |
|-----------|------|--------------|
| `hailango-user-uploads-{env}` | ユーザーアップロード | 30日→NEARLINE, 90日→COLDLINE |
| `hailango-audio-cache-{env}` | TTS音声キャッシュ | 7日で自動削除 |
| `hailango-ocr-cache-{env}` | OCR結果キャッシュ | 30日で自動削除 |

### 予算アラート
- 50% 到達時: メール通知
- 80% 到達時: メール通知
- 90% 到達時: メール通知
- 100% 到達時: メール通知
- 予測で100%超過時: メール通知

### サービスアカウント
- 名前: `hailango-api-sa@hailango.iam.gserviceaccount.com`
- 付与されるロール:
  - `roles/cloudvision.user`
  - `roles/cloudtranslate.user`
  - `roles/storage.objectAdmin`
  - カスタムロール: TTS/STT 使用権限

---

## 6. 環境変数（.env に追加）

Terraform 適用後、以下の環境変数が利用可能になります：

```bash
# GCP 設定
GOOGLE_CLOUD_PROJECT=hailango
GOOGLE_CLOUD_REGION=asia-northeast1
GOOGLE_APPLICATION_CREDENTIALS=./infrastructure/credentials.json

# API キー（クライアント用、制限付き）
GOOGLE_CLOUD_API_KEY=<terraform output で取得>

# Cloud Storage
GCS_USER_UPLOADS_BUCKET=hailango-user-uploads-development
GCS_AUDIO_CACHE_BUCKET=hailango-audio-cache-development
GCS_OCR_CACHE_BUCKET=hailango-ocr-cache-development

# レート制限
RATE_LIMIT_OCR=60
RATE_LIMIT_TTS=100
RATE_LIMIT_STT=60
```

---

## 7. トラブルシューティング

### 認証エラー
```bash
# 認証情報のリフレッシュ
gcloud auth login --update-adc

# 認証状態の確認
gcloud auth list
```

### Terraform エラー
```bash
# 状態のリフレッシュ
terraform refresh

# 強制的に再初期化
rm -rf .terraform
terraform init
```

### API が有効化されない
```bash
# 手動で API を有効化
gcloud services enable vision.googleapis.com
gcloud services enable texttospeech.googleapis.com
# ...
```

### 請求先アカウントがリンクされていない
```bash
# 請求先アカウント一覧
gcloud billing accounts list

# プロジェクトにリンク
gcloud billing projects link hailango --billing-account=XXXXXX-XXXXXX-XXXXXX
```

---

## 8. 参考リンク

- [GCP Console](https://console.cloud.google.com/?project=hailango)
- [API Dashboard](https://console.cloud.google.com/apis/dashboard?project=hailango)
- [Billing](https://console.cloud.google.com/billing?project=hailango)
- [Storage](https://console.cloud.google.com/storage?project=hailango)
- [IAM](https://console.cloud.google.com/iam-admin?project=hailango)

---

## 9. 注意事項

1. **credentials.json は絶対に Git にコミットしない**（.gitignore に含まれています）
2. **terraform.tfvars も Git にコミットしない**（機密情報が含まれる可能性）
3. **予算アラートを設定してコスト監視を行う**
4. **開発時は `USE_MOCK_APIS=true` でモックを使用してコスト削減**

---

このドキュメントを参照して、ローカル環境で続きを実行してください。
質問があれば、Claude Code で「引き継ぎ.md を参照して」と伝えてください。
